/** Compatibility re-export for public hosted-agent pricing display helpers. */
export {
  estimateHoursRemaining,
  formatDuration,
  formatHourlyRate,
  formatMonthlyEstimate,
  formatUSD,
  MONTHLY_IDLE_COST,
  MONTHLY_RUNNING_COST,
  packSavingsPercent,
} from "@elizaos/cloud-sdk/browser-contracts";
